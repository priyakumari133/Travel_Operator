import React from "react";
import BookingList from "../components/BookingList";

const Bookings = () => (
  <div>
    <h2 style={{ fontSize: "24px", marginBottom: "20px" }}>Bookings</h2>
    <BookingList />
  </div>
);

export default Bookings;
