import React from "react";
import PaymentStatus from "../components/PaymentStatus";

const Payments = () => (
  <div>
    <h2 style={{ fontSize: "24px", marginBottom: "20px" }}>Payment Status</h2>
    <PaymentStatus />
  </div>
);

export default Payments;
